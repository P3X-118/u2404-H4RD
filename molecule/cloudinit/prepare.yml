---
- name: Prepare
  hosts: all
  gather_facts: true
  become: true

  tasks:
    - name: Update apt cache
      ansible.builtin.apt:
        update_cache: yes
        cache_valid_time: 3600

    - name: Install basic dependencies
      ansible.builtin.apt:
        name:
          - python3
          - python3-pip
          - sudo
          - curl
          - gnupg
          - git
          - jq
          - openssh-client
          - cloud-init
          - cloud-utils
        state: present

    - name: Setup cloud-init seed directory
      ansible.builtin.file:
        path: /var/lib/cloud/seed/nocloud
        state: directory
        mode: '0755'
        owner: root
        group: root

    - name: Create cloud-init user-data
      ansible.builtin.copy:
        content: |
          #cloud-config
          ssh_pwauth: true
          users:
            - name: ronon
              primary_group: ronon
              groups: sudo
              sudo: ALL=(ALL) NOPASSWD:ALL
              shell: /bin/bash
              lock_passwd: false
              password: "$6$QaHca4xL6qvsRVjh$b8ms6YHT8fqOPiMH4AAFpUzG3zQfc9SLd8NPoXWg3ZpQo6l9TrqJdgpklUbDC.QLXLkOvRna2j6SwhVrpSkbg/"
              ssh_authorized_keys:
                - ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIBCwqtId82CDe7yxOpF29R2Tem2T4jSbduLoL4JhZf+a
          packages:
            - vim
            - curl
            - wget
        dest: /var/lib/cloud/seed/nocloud/user-data
        mode: '0644'
        owner: root
        group: root

    - name: Create cloud-init meta-data
      ansible.builtin.copy:
        content: |
          instance-id: molecule-cloudinit
          local-hostname: ubuntu-hardened
        dest: /var/lib/cloud/seed/nocloud/meta-data
        mode: '0644'
        owner: root
        group: root
